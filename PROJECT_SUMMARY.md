# מערכת ניהול שירים ותוכן - סיכום פרויקט

## סקירה כללית

פרויקט זה מאחד שתי מערכות ניהול תוכן נפרדות למערכת אחת מקיפה ומאוחדת לניהול שירים, לוגואים, פלאגינים ותוכן לרשתות חברתיות.

## מטרות הפרויקט

✅ **מיזוג שתי מערכות** - איחוד המערכת הראשית (social_management) עם מערכת השירים (my_songs_db)  
✅ **עיצוב אחיד** - יישום פלטת צבעים ופונט Almoni על כל המערכת  
✅ **פונקציונליות מורחבת** - הוספת תמיכה בקישורי Google Drive, YouTube, ZIP של מילים ועוד  
✅ **מודולים חדשים** - לוגואים, פלאגינים, תוכן לסטוריז  

---

## מבנה המערכת

### מסד נתונים מאוחד: `songs_management`

#### טבלאות ניהול משתמשים
- `roles` - תפקידים (admin, editor, viewer)
- `users` - משתמשים עם הרשאות
- `user_permissions` - הרשאות מתקדמות

#### טבלאות תוכן ראשיות
- `songs` - שירים (מאוחד משתי המערכות)
  - title, title_he, title_en
  - artist, album
  - youtube_link, google_drive_link, file_path
  - lyrics, xml_content
  - category_id, tags
  
- `song_images` - תמונות מילים לשירים
  - image_path, page_number
  - image_type (lyrics_page, cover, other)
  
- `logos` - לוגואים למפיקים ואירועים
  - producer_name_heb, producer_name_eng
  - logo_path, logo_type
  - download_count

- `plugins` - פלאגינים לתוכנות עריכה
  - plugin_name, version
  - file_path, file_size
  - plugin_type (audio, video, graphics)
  - compatible_software

- `story_content` - תוכן לסטוריז
  - title, content_type
  - music_file_path, logo_id, background_image
  - duration, template_name
  - tags, description

#### טבלאות נוספות
- `categories` - קטגוריות לשירים
- `song_tags` - תגיות
- `clients` - לקוחות
- `social_posts` - פוסטים לרשתות חברתיות
- `activity_log` - יומן פעילות

---

## מודולים עיקריים

### 1. ניהול שירים (`modules/songs/`)

**קבצים:**
- `list.php` - רשימת שירים עם DataTables
- `add.php` - הוספת שיר חדש
- `edit.php` - עריכת שיר
- `delete.php` - מחיקת שיר
- `upload.php` - drag & drop upload
- `download_zip.php` - הורדת ZIP של מילים

**תכונות:**
- ✅ העלאת MP3/WAV עד 50MB
- ✅ קישורי Google Drive (חלופה להעלאה)
- ✅ קישורי YouTube
- ✅ העלאת תמונות PNG מרובות (מילים)
- ✅ יצירת ZIP אוטומטי של כל תמונות המילים
- ✅ מטא-דאטה מלאה: אמן, אלבום, משך זמן
- ✅ תגיות וקטגוריות
- ✅ ספירת הורדות

### 2. ניהול לוגואים (`modules/logos/`)

**קבצים:**
- `list.php` - רשימת לוגואים
- `add.php` - הוספת לוגו
- `upload.php` - drag & drop upload
- `delete.php` - מחיקת לוגו

**תכונות:**
- ✅ תמיכה ב-PNG, SVG, JPG
- ✅ שמות בעברית ואנגלית
- ✅ סיווג לפי סוג (מפיק, מותג, אירוע)
- ✅ drag & drop upload מהיר
- ✅ תצוגה מקדימה

### 3. ניהול פלאגינים (`modules/plugins/`)

**קבצים:**
- `list.php` - רשימת פלאגינים
- `upload.php` - drag & drop upload
- `delete.php` - מחיקת פלאגין

**תכונות:**
- ✅ זיהוי אוטומטי של סוג פלאגין
- ✅ תמיכה בכל הפורמטים (VST, VST3, AU, AAX, וכו')
- ✅ ניהול גרסאות
- ✅ רשימת תוכנות תואמות
- ✅ מעקב גודל קבצים

### 4. ניהול תוכן לסטוריז (`modules/stories/`)

**קבצים:**
- `list.php` - רשימת תוכן
- `add.php` - יצירת תוכן חדש
- `get_story.php` - API לתצוגה מקדימה
- `delete.php` - מחיקה

**תכונות:**
- ✅ שילוב מוזיקה + לוגואים + תמונות רקע
- ✅ ניהול תבניות
- ✅ תגיות לארגון
- ✅ תצוגה מקדימה
- ✅ ניהול משך זמן (duration)
- ✅ ספירת שימושים

---

## מערכת העיצוב

### פלטת צבעים

```css
--primary: #FF6B2B         /* כתום ראשי */
--primary-light: #FF8F5E   /* כתום בהיר */
--primary-dark: #E85A1F    /* כתום כהה */
--secondary: #2D3142       /* אפור/כחול כהה */
--secondary-light: #4A4F63 /* אפור בהיר */
--background: #F5F6FA      /* רקע */
--surface: #FFFFFF         /* משטחים */
```

### טיפוגרפיה

- **פונט ראשי**: Almoni (עברית)
- **פונט משני**: Arial, sans-serif
- **משקלים**: 200, 300, 400, 700

### אלמנטים

- **Border Radius**: 8px (sm), 12px (md), 16px (lg)
- **Shadows**: קלות, בינוניות, כבדות
- **RTL Support**: כיוון ימין-לשמאל מלא

---

## תכונות אבטחה

### הגנות מיושמות

✅ **File Upload Security**
- בדיקת סוג קובץ (MIME type)
- הגבלת גודל קובץ
- שמות קבצים אקראיים (timestamp + uniqid)
- ניקוי אוטומטי של קבצי ZIP ישנים

✅ **XSS Protection**
- `htmlspecialchars()` על כל output
- `escapeHtml()` ב-JavaScript
- Input validation עם `filter_input()`

✅ **SQL Injection Prevention**
- Prepared statements בלבד
- PDO עם parameterized queries
- No raw SQL concatenation

✅ **Input Validation**
- Range validation (ID > 0)
- Type validation (INT, STRING, etc.)
- Required field checks

---

## API Endpoints

### Songs
- `GET /api/v1/search.php` - חיפוש שירים

### Stories
- `GET /modules/stories/get_story.php?id=X` - פרטי סטורי

### Upload Endpoints
- `POST /modules/songs/upload.php` - העלאת שירים
- `POST /modules/logos/upload.php` - העלאת לוגואים
- `POST /modules/plugins/upload.php` - העלאת פלאגינים

### Download Endpoints
- `GET /modules/songs/download_zip.php?id=X` - הורדת ZIP מילים

---

## תיקיות Upload

```
public/uploads/
├── songs/          # קבצי שירים (MP3, WAV)
├── logos/          # קבצי לוגו (PNG, SVG, JPG)
├── plugins/        # קבצי פלאגינים
├── images/         # תמונות מילים לשירים
├── story_content/  # תוכן לסטוריז (מוזיקה, תמונות)
└── temp/           # קבצי ZIP זמניים
```

כל התיקיות מוגדרות ב-`.gitignore` כדי למנוע commit של קבצים גדולים.

---

## טכנולוגיות

### Backend
- **PHP 7.4+** - שפת שרת
- **MySQL 5.7+** - מסד נתונים
- **PDO** - חיבור למסד נתונים

### Frontend
- **Bootstrap 5.3** - framework UI
- **Bootstrap Icons** - אייקונים
- **DataTables** - טבלאות מתקדמות
- **jQuery 3.6** - manipulations

### אחר
- **ZipArchive** - יצירת קבצי ZIP
- **FileReader API** - preview של קבצים

---

## הוראות התקנה

### 1. דרישות
- PHP 7.4+ עם הרחבות: PDO, MySQL, ZIP
- MySQL/MariaDB 5.7+
- Apache/Nginx עם mod_rewrite
- 100MB+ שטח דיסק

### 2. הקמת מסד נתונים

```bash
mysql -u username -p < database/schema.sql
```

או דרך phpMyAdmin:
1. צור DB בשם `songs_management`
2. Import את `database/schema.sql`

### 3. הגדרת הרשאות

```bash
chmod 755 -R public/uploads/
```

### 4. משתמש ראשון

- משתמש: `admin`
- סיסמה: `123456`
- **שנה מיד אחרי כניסה ראשונה!**

---

## תיעוד

### למשתמשים
📖 **MANUAL.md** - מדריך משתמש מקיף בעברית
- הוראות שימוש לכל מודול
- טיפים ועצות
- שאלות נפוצות
- פתרון בעיות

### למפתחים
📖 **README_DB.md** - תיעוד טכני
- סכמת מסד נתונים
- הסבר על כל טבלה
- קישורים בין טבלאות
- דוגמאות שאילתות

---

## תחזוקה

### Backup מומלץ

**מסד נתונים (יומי/שבועי):**
```bash
mysqldump -u username -p songs_management > backup_$(date +%Y%m%d).sql
```

**קבצים (שבועי):**
```bash
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz public/uploads/
```

### ניקוי אוטומטי

המערכת מנקה אוטומטית:
- ✅ קבצי ZIP ישנים (מעל 24 שעות) ב-`uploads/temp/`

מומלץ להוסיף cron job:
```cron
0 3 * * * php /path/to/cleanup.php
```

---

## שיפורים עתידיים אפשריים

### פונקציונליות
- [ ] מערכת תגובות/דירוגים לשירים
- [ ] שיתוף ישיר לרשתות חברתיות
- [ ] עריכת תמונות מילים (crop, resize)
- [ ] המרת פורמטים אוטומטית
- [ ] API REST מלא
- [ ] webhook notifications

### ביצועים
- [ ] CDN לקבצים סטטיים
- [ ] Redis/Memcached לcaching
- [ ] Lazy loading לתמונות
- [ ] Compression אוטומטי

### אבטחה
- [ ] Two-Factor Authentication
- [ ] Rate limiting
- [ ] CAPTCHA בהעלאות
- [ ] Virus scanning לקבצים

---

## רישיון

MIT License - ראה קובץ LICENSE

---

## תמיכה

- GitHub Issues
- Documentation: MANUAL.md, README_DB.md
- Email: support@example.com

---

**גרסה:** 1.0.0  
**תאריך:** דצמבר 2024  
**מפתח:** Bunoto Development Team
